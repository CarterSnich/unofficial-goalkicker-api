{
	"version": 2,
	"installCommand": "npm install",
	"builds": [
		{
			"src": "./bin/www",
			"use": "@now/node"
		}
	],
	"rewrites": [
		{
			"source": "/api/(.*)",
			"destination": "/api"
		}
	]
}
